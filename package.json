{
  "name": "pi-clickable-paths",
  "version": "1.2.0",
  "description": "OSC 8 hyperlinks on file paths in tool headings and assistant messages (adapts to fff/bash-image overrides)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test \"tests/*.test.ts\""
  },
  "keywords": ["pi-package"],
  "main": "extensions/clickable-paths.ts",
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.9.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/1am2syman/pi-clickable-paths.git"
  },
  "license": "MIT"
}
